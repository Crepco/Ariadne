# Runtime dependencies. Prefer an editable install (`pip install -e .`), which
# reads pyproject.toml; this file mirrors it for `pip install -r requirements.txt`.

neo4j>=5.14            # Neo4j Python driver (generator, tools, scoring)
python-dotenv>=1.0     # load Neo4j creds + API keys from .env
requests>=2.31         # OpenRouter (OpenAI-compatible) HTTP client
pandas>=2.0            # metrics aggregation
matplotlib>=3.7        # scaling plots

# Optional native Gemini backend (only if you use LLM_PROVIDER=gemini):
# google-genai>=1.0
